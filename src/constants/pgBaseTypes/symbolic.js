export const symbolic = {
  VARCHAR: { key: 'VARCHAR', params: { length: { key: 'length', min: 1 } } },
  CHAR: { key: 'CHAR', params: { length: { key: 'length', min: 1 } } },
  TEXT: { key: 'TEXT' }
}
