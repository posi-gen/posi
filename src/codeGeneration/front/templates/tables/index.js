const { createForm, getFormFileName } = require('./form')
const { createList, getListComponentName } = require('./list')
const { createPage, getPageComponentName } = require('./page')

module.exports = {
  createForm,
  createList,
  createPage,
  getFormFileName,
  getListComponentName,
  getPageComponentName
}
